SHViT (S1..S4) fine-tuned on Flowers102 from ImageNet-pretrained checkpoints.

Run the Stage 3 notebook (or `finetune_shvit_flowers102.py`) once per variant
to fill in the per-model results — outputs (`training_log.csv`, `best.pth`,
`checkpoint_*.pth`) are written under
`CV_Research_Paper_Flowers102/Stage 3: fine-tuning SHViT/<shvit_variant>/`.
